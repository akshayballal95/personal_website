/**
 * Site feature flags.
 *
 * CHAT_ENABLED turns the AI assistant on/off everywhere at once: the nav
 * entries, the home page CTA, the /chat route, and the /api/chat endpoint.
 * Flip it back to `true` to re-enable the feature — no other edits needed.
 */
export const CHAT_ENABLED = false;
